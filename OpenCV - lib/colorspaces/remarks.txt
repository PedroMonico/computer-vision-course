An image in openCV using imread, every Pixel  is in the BGR (Blue Green Red) color space.

To convert from one colorspace to another -> cv2.cvtColor(img, cv2.COLOR_BGR2RGB).   Basically, we are changing red and blue.